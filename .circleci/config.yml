# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.0

jobs:
  build:
    docker:
      - image: node:10
    steps:
      - checkout
    test:
      machine:
        enabled: true
      steps:
        - run: echo "hello cicleci"
workflows:
  version: 2
  test-hello:
    jobs:
      - test
